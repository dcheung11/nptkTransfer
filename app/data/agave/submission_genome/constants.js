const SCOPE = 'data/agave/submission_genome/';

export const DEFAULT_ACTION = `${SCOPE}DEFAULT_ACTION`;

export const DEPOSIT_GENOME = `${SCOPE}DEPOSIT_GENOME`;

export const SUCCESS_DEPOSIT_GENOME = `${SCOPE}SUCCESS_DEPOSIT_GENOME`;

export const FAILED_DEPOSIT_GENOME = `${SCOPE}FAILED_DEPOSIT_GENOME`;
